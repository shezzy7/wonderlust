<% layout("/layout/boilerplate") %>
    <div class="row mt-3 mb-3">
        <div class="col-6 offset-3">
            <h2>Signup for Wanderlust</h2>
            <form action="/signup" novalidate class="needs-validation" method="POST">

                <div class="mb-3">
                    <label for="username" class="form-label">username </label>
                    <input type="text" placeholder="Enter username" name="username" class="form-control" required>
                    <!-- bootsrap also provides us class with the help of which of we can show some message if user enter a valid data and alos can show a message when user enter in invalid data or doesn't enter any data in a require fiels.We do this the help of following classes. -->
                    <div class="valid-feedback">
                        Title looks good!
                    </div>
                    <div class="invalid-feedback">
                        Please enter a title
                    </div>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label">email </label>
                    <input type="email" placeholder="Enter email" name="email" class="form-control" required>
                    <!-- bootsrap also provides us class with the help of which of we can show some message if user enter a valid data and alos can show a message when user enter in invalid data or doesn't enter any data in a require fiels.We do this the help of following classes. -->
                    
                </div>
                <div class="mb-3">
                    <label for="phone" class="form-label">Whatsapp No </label>
                    <input type="number" placeholder="Enter mobile number" name="phone" class="form-control" required>
                    <!-- bootsrap also provides us class with the help of which of we can show some message if user enter a valid data and alos can show a message when user enter in invalid data or doesn't enter any data in a require fiels.We do this the help of following classes. -->
                    
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">password </label>
                    <input type="password" placeholder="Enter password" name="password" class="form-control" required>
                    <!-- bootsrap also provides us class with the help of which of we can show some message if user enter a valid data and alos can show a message when user enter in invalid data or doesn't enter any data in a require fiels.We do this the help of following classes. -->
                    
                </div>
                <button class="btn btn-success">Submit</button>
            </form>
            <p>Already have an account?<a href="/login">Sign In</a></p>
        </div>
    </div>
