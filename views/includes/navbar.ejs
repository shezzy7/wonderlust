<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Responsive Navbar</title>

  <!-- Bootstrap CSS -->
 
  <style>
    /* Custom Styles for Testing */
    .btn-search {
      background-color: #fe424d;
      color: #ffffff;
      border-radius: 25px;
      padding: 0 1rem 0 1rem;
    }

    .btn-search:hover {
      background-color: #fe424d;
      color: #ffffff;
      border-radius: 25px;
      padding: 0 1rem 0 1rem;
    }

    .btn-search i {
      display: inline;
    }

    .search-inp {
      border-radius: 25px;
      padding: 0.5rem 3rem 0.5rem 3rem;
      font-size: 1rem;
    }
  </style>
</head>
<body>

<!-- Navbar -->
<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore All</a>
        <a class="nav-link" href="/listings/new">Add New</a>
      </div>
      <div class="navbar-nav ms-auto">
        <form class="d-flex needs-validation" action="/listings/search" method="get">
          <input class="form-control me-2 search-inp" name="search" type="search" placeholder="Search country" aria-label="Search" required>
          <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>
      <!-- User-specific Links -->
      <div class="navbar-nav ms-auto">
        <!-- Assuming you have a variable currUser to check if user is logged in -->
        <!-- If currUser is undefined or null, show login/signup links -->
        <% if (!currUser) { %>
          <a class="nav-link" href="/signup"><b>Sign up</b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
        <% } else { %>
          <a class="nav-link me-2" href="/listings">Hello, <b><%= currUser.username %></b></a>
          <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<!-- Bootstrap JS -->

</body>
</html>
